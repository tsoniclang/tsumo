{
  "name": "tsumo-cli",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "build": "tsonic build",
    "build:aot": "tsonic build -c tsonic.aot.json",
    "dev": "tsonic run -- --help"
  },
  "devDependencies": {
    "@tsumo/engine": "^0.0.0"
  }
}
