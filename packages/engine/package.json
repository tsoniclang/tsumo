{
  "name": "@tsumo/engine",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    },
    "./index.js": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "dist/**/*.d.ts",
    "src/**/*.ts",
    "tsonic.package.json"
  ],
  "scripts": {
    "restore": "${TSONIC_BIN:-tsonic} restore",
    "typecheck": "${TSONIC_BIN:-tsonic} restore && tsc -p tsconfig.json",
    "build": "${TSONIC_BIN:-tsonic} restore && ${TSONIC_BIN:-tsonic} build",
    "test": "${TSONIC_BIN:-tsonic} test"
  }
}
